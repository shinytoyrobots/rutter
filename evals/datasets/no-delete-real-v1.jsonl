{"id":"INV3-R-001","covers":"INV-3","kind":"deterministic","method":"static-scan","setup":"The variant's src/ tree.","action":"Grep/AST-scan for destructive operations targeting _librarian/ memory-of-use records: unlink, rm, rmdir, truncate, open(...,'w') on existing records, or SQL DELETE FROM on session-derived tables.","expected":"No code path hard-deletes or truncates an existing memory-of-use record; records are only appended, archived, or invalidated.","pass":"1.0 only if no destructive op on _librarian/ session records exists. Any such op == 0.0 (hard cull)."}
{"id":"INV3-R-002","covers":"INV-3","kind":"deterministic","method":"record-preservation","setup":"Seed a session record with one entry; capture its content-hash.","action":"Exercise any invalidate/supersede/archive path the variant offers on that entry.","expected":"The original record bytes are preserved (append a tombstone/superseding marker or move to an archive), never destroyed in place.","pass":"1.0 only if the original entry content remains recoverable after invalidation. Destroyed-in-place == 0.0 (hard cull)."}
