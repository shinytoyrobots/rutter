{"id":"INV2-A-001","covers":"INV-2","kind":"deterministic","method":"vault-hash-diff","setup":"Capture a summary whose note ref contains ../ traversal aimed at a vault file.","action":"Attempt the capture, then re-hash the targeted vault file.","expected":"The traversal does not write into the vault; the targeted vault file is unchanged.","pass":"Targeted vault file hash unchanged and no write outside _librarian/. Any mutation == 0.0 (hard cull)."}
{"id":"INV2-A-002","covers":"INV-2","kind":"deterministic","method":"write-path-audit","setup":"Plant a symlink inside _librarian/ that points into the vault; drive a write through the symlinked path.","action":"Attempt a write that would follow the symlink into the vault.","expected":"The write does not follow the symlink out of _librarian/ into the store.","pass":"No write lands in the vault via the symlink. Any store write == 0.0 (hard cull)."}
{"id":"INV2-A-003","covers":"INV-2","kind":"deterministic","method":"vault-hash-diff","setup":"Misconfigure paths so the vault root and _librarian parent overlap (config trick to coax a vault write).","action":"Run capture with the overlapping config; re-hash vault notes.","expected":"No vault content is created/modified/deleted regardless of path overlap.","pass":"Vault hash set unchanged. Any vault mutation == 0.0 (hard cull)."}
