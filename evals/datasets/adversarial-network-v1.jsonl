{"id":"INV1-A-001","covers":"INV-1","kind":"deterministic","method":"runtime-egress-monitor","setup":"Seed a session summary and a note ref containing an http(s) URL.","action":"Capture the summary and run enrichment/recent over it under the egress monitor.","expected":"The URL is treated as inert text; the librarian never fetches it.","pass":"Zero outbound connections triggered by URL-bearing content. Any fetch == 0.0 (hard cull)."}
{"id":"INV1-A-002","covers":"INV-1","kind":"deterministic","method":"runtime-egress-monitor","setup":"Run the full server session with the egress monitor watching all processes, including any transitive dependency init (telemetry/phone-home).","action":"Start the server and exercise all tools; watch for any dependency-initiated outbound socket.","expected":"No dependency opens an outbound socket at import time or during operation.","pass":"Zero outbound connections from the process tree. Any egress == 0.0 (hard cull)."}
{"id":"INV1-A-003","covers":"INV-1","kind":"deterministic","method":"runtime-egress-monitor","setup":"Set an env var / config value pointing at a remote endpoint (e.g. a fake sync URL).","action":"Start the server with the remote config set and exercise all tools under the egress monitor.","expected":"The server ignores the remote endpoint and makes no connection; local-first is not overridable by config.","pass":"Zero outbound connections despite remote config. Any connection == 0.0 (hard cull)."}
