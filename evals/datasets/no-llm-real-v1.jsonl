{"id":"INV6-R-001","covers":"INV-6","kind":"deterministic","method":"dependency-scan","setup":"The variant's package.json + lockfile + src/ imports.","action":"Scan for LLM/model-inference SDKs and runtimes (openai, @anthropic-ai/sdk used for inference, langchain, llamaindex, ollama, node-llama-cpp, @xenova/transformers, transformers.js, onnxruntime-node) wired into any server code path.","expected":"No model-inference dependency is imported or invoked by the server; the server is code + storage only.","pass":"1.0 only if no inference SDK/runtime is on a server code path. Any inference dependency in use == 0.0 (hard cull)."}
{"id":"INV6-R-002","covers":"INV-6","kind":"deterministic","method":"runtime-inference-audit","setup":"Run capture, librarian-recent, and enrichment over a temp vault.","action":"Trace execution for any model-inference call; confirm the stored summary is the client-provided curated line, stored verbatim.","expected":"No inference occurs in the server; summary generation is the client's, and the server persists the pre-made line as-is.","pass":"1.0 only if zero inference calls occur and the summary is stored verbatim from client input. Any server-side inference == 0.0 (hard cull)."}
