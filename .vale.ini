StylesPath = .vale/styles
MinAlertLevel = suggestion
Vocab = Librarian

# Uses Vale's built-in base style (no `vale sync` / network needed).
[*.md]
BasedOnStyles = Vale

# Code identifiers and product names that are not spelling errors.
Vale.Terms = NO
